spring:
  application:
    name: unix

  datasource:
    url: jdbc:mysql://localhost:3306/UniX
    username: root
    password: P@ssword

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      "[hibernate.format_sql]": true